{
  "name": "@desource/image-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf .svelte-kit build .vercel test-results",
    "clean:modules": "rimraf node_modules",
    "prepare": "svelte-kit sync",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@desource/image": "workspace:*",
    "@desource/image-svelte": "workspace:*",
    "ipx": "^3.1.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "^6.3.4",
    "@sveltejs/kit": "^2.70.2",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.56.9",
    "svelte-check": "^4.7.5"
  }
}
